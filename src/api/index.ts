export { api } from './client'

// Endpoint modules — import individually:
//   import { login, getCurrentUser } from '@/api/endpoints/auth'
//   import { getFeatures } from '@/api/endpoints/features'
// or use the wildcard:
//   import * as authApi from '@/api/endpoints/auth'
